# Hermes 3.1.5

- 对话三点菜单移除对话详情、快捷命令。加号菜单的链接入口改为命令，复用快捷命令选择器。
- 空间支持选择附件，点击最近产物或项目文件后返回原对话，保留文字及已有附件。取消不改变草稿；读取失败可重试。
- 修复相对文件链接被识别为错误绝对路径、扩展名被截短、文件链接编码及旧产物索引路径问题；读取明确绑定来源档案。
- 回看新建按钮从 56dp 缩小为 45dp，保留 48dp 点击区域。
- 底部选中图标采用蓝紫渐变，未选中保持灰色线性样式，助理图标保留女孩轮廓；继续保留轻微缩放和颜色过渡，无图标底色。
- 桌面图标换为白底托腮女孩，保留自适应图标安全边距。
- 底部固定区域上方添加一条细的同色系分隔线。

本包为可覆盖此前同签名测试版的 APK。完整验证记录见 VALIDATION-3.1.5.md。
