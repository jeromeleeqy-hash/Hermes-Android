# Hermes Android 2.8.1

发布日期：2026-08-18

本次升级集中处理移动端细节、流式回复可靠性和版本资料一致性。构建版本为 `2.8.1-release`，`versionCode 281`。

## 界面与文案

- 会话置顶图标改为更修长的图钉，减少原图标矮胖、横向占位过重的问题。
- “我的”签名前和远程网关主连接按钮内不再显示绿色对号。
- 任务页右上角新建入口改为与刷新按钮协调的小型圆形按钮。
- “长忆录 / 心智章程”统一改名为“我的记忆 / 我的心智”。
- 对话列表不再展示 Cron 会话；Cron 运行会话统一放在任务页“执行记录”，点按可查看完整聊天内容。
- 移除聊天中的“Hermes 正在整理结果”面板，运行详情继续在任务页集中呈现。
- 新会话空白页、空对话列表和使用说明顶部加入耳机女孩循环动画。
- 使用说明取消目录、章节和内容卡片的数字编号。

## 头像大图

- 沉浸式头像取消局部模糊，保留清晰原图和底部渐暗遮罩。
- 姓名、签名和三个操作按钮叠放在同一照片区域，按钮保留轻量半透明表面。
- 在照片区域向上滑动可以直接返回“我的”页面，原有左上角返回按钮继续保留。

## 流式与交互恢复

- 实时流静默 45 秒后开始后台核对当前会话；如果服务端已经生成最终回复，客户端自动同步并结束加载。
- 实时连接失效时进入服务端结果取回流程，最长等待窗口从约 1 分钟扩展到约 4 分钟。
- 审批和澄清请求等待用户时不会被恢复超时误删；请求和运行快照使用现有加密存储，进程重启后可继续处理。
- 响应恢复后的审批或澄清请求后，客户端继续轮询并同步本轮最终结果。
- 补充工具失败事件和失败状态消息兼容。

## 参考来源

- 来源按正文出现顺序汇总，不再固定先列 Markdown 链接、后列普通 URL。
- 支持 Markdown 引用式链接和 URL 路径中的成对括号。
- 代码块、行内代码和 Markdown 图片地址不会误入参考来源卡片。

## 工程资料

- Gradle 构建版本、应用内更新日志、README、交接状态、开发指南和网络 User-Agent 已统一为 2.8.1。
- 源码包保留原有固定开发签名和 Android 8.0（API 26）最低版本要求。
