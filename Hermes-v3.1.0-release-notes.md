# Hermes 3.1.0 UI 重构

这次将已确认的助理界面方向落地到原生 Jetpack Compose 前端，没有替换为网页壳。

## 页面与交互

- 打开已连接的 App 进入助理首页。展示真实待确认请求、运行中的对话、近期会话预览；无数据时保留简洁空状态。
- 首页输入文字后点击“开始聊”，创建当前项目下的新会话并填入草稿，确认发送仍在聊天页。附件、图片、单次语音、连续语音沿用聊天页原入口。
- 底部为“助理 / 回看 / 我的”。回看保留项目、Profile、搜索、置顶、归档和管理；首页提供任务与定时、文件与成果入口。
- 聊天增加“正在处理（或最新答复） / 完整对话”。集中阅读显示真实运行状态、Agent 返回的计划、工具记录、最新答复和文件，完整历史保留原消息能力。
- 待确认请求共用独立面板，选中后确认提交；多选与自定义回答沿用原协议。长选项滚动，底部确认区固定。
- 标题可点击查看全文；圆角、背景、主要文字层级和按钮对比度统一调整，保留深色模式。

## 数据和兼容

不写入演示业务数据，不制造执行百分比，不将普通聊天自动包装成任务。旧会话、草稿、项目、附件、语音、定时、设置和后台并行运行沿用现有实现。

本次提供的是测试包：包名 com.qingyu.hermescompanion.debug，versionCode 310。使用此前交付测试版同一签名，可覆盖此前 3.0.4a 测试版。原正式版的私钥未包含在源码中，不能据此保证覆盖原正式包。源码排除了密钥、构建缓存和本地 SDK 路径。

## 验收范围

构建和自动回归结果见 VALIDATION-3.1.0.md。本环境没有连接 Android 真机、模拟器或你的远程网关，不把此前 HTML 原型的视觉测试当作原生 App 的真机验收。

建议安装后依次检查：首页开新对话和项目目录、两个对话并行运行、返回首页、长答复及表格、12 项选择与键盘、文件和语音、深色与放大字体。
