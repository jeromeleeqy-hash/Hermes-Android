# Hermes Android 2.7.0

发布日期：2026-08-17

本次升级把已审核的八轮精细图标完整接入客户端，并重新校准不同页面的图标尺度、触控范围和语义色。

## 全量图标系统

- 236 个 256 × 256 SVG 母版转换为 Android 原生 VectorDrawable，覆盖底栏、首页、会话、聊天、空间、任务、个人主页、设置、编辑器和状态反馈。
- 业务代码不再引用旧位图或旧版矢量，统一通过 `HermesIconKind` 使用语义图标。
- 线性图标使用闭合填充路径表达轮廓，解决小尺寸下可能出现的断点、重叠和交叉毛刺。
- 底栏保留线性/实体双态过渡；选中状态与滑动底板、颜色和缩放动画同步。

## 尺寸与布局

- 底栏图标统一为 22 dp；设置和主要列表图标使用 19–20 dp；紧凑操作使用 17–18 dp；微型状态使用 12–16 dp。
- 图形尺寸与触控区域分离，按钮仍保留 40–48 dp 的易点击范围，避免为了清爽而牺牲可用性。
- Markdown、网页、图片、文件夹、来源会话、保存、分享和下载改用专属图形，文件和操作含义更明确。

## 配色

- 中性灰承担普通导航与辅助操作；蓝、青、紫、绿、琥珀和红只用于明确的动作、状态与文件类型。
- 图标自动适配浅色和深色模式，彩色设置底板上的图形统一使用白色。

## 工程维护

- 新增 `tools/import_refined_icons.py`，可从设计母版重复生成 Android 资源并检查重名、异常视口和过期资源。
- 更新图标系统文档，明确不同界面的尺寸等级和新增图标接入规范。
