# Hermes 3.1.1：参考图视觉还原

## 本次调整

- 使用用户给定首页设计图中的原始女孩画面：原图字节不改，在 Compose 中绘制 x=493、y=176、312×267 的画面区域，保留托腮姿势、发型、耳机和青色背景。不使用重新生成的候选素材。
- 首页重新对齐头像与名称、右侧搜索、问候和女孩、白色主卡片、双色选项、合并的近期事项、轻量状态行和紧凑底部输入。
- 底部恢复助理（房屋）、回看（时钟）、我的（人物）图标，去掉大面积选中底色。
- 有待确认请求时主卡片展示真实问题和选项；没有请求时展示近期会话，避免制造虚假审批。主卡片和列表使用同一组排版边界，不依赖特定运营话题。
- 工作详情去掉常驻的大型切换条和模型状态行，完整对话、模型设置、命令及连续语音仍在右上菜单。以真实步骤绘制时间线，资料放入同一组列表，运行中保留调整要求和停止入口。这里的“停止”对应现有停止接口，不冒充可恢复的暂停。
- 首页附件按钮创建当前项目的新会话后打开附件工具；语音按钮创建后触发已有语音流程。文本保留到新会话草稿。搜索返回首页，不再绕回会话列表。

## 键盘问题

3.1.0 首页的 Scaffold 为底部导航预留空间，子页面又独立使用 IME padding。未消费的底部占位会叠加到键盘之上。

3.1.1 在首页键盘可见时收起导航，并在应用 Scaffold padding 后消费相应 WindowInsets，再应用键盘 inset。输入条仅保留 4dp 外边距。

## 验证与覆盖升级

验证结果见 VALIDATION-3.1.1.md。原生 Compose 渲染测试使用 Robolectric 和受控数据，键盘测试注入明确的 IME 占位，不假装连接了用户的真机或输入法。

本测试包使用此前测试包同一签名，包名 com.qingyu.hermescompanion.debug，versionCode 311，可覆盖之前提供的 3.1.0 测试版。请直接覆盖安装，无需卸载。
