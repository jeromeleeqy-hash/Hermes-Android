# Hermes 3.1.3

本次围绕整套 UI 的一致性优化主题、开关、创建按钮、导航图标、卡片与页面密度。颜色集中从主题获取；浅色页面以冰蓝、白色、少量薄荷和淡紫为主，设置图标取消高饱和彩色底座。开关统一白色滑块。

底部导航为助理、回看、文件、我的，未选中描边，选中实体，不使用图标底色。保留弹性缩放与渐变，页面淡入上移，点击与创建按钮有反馈。执行中心可直接从助理页顶部进入。

文件路径规则：底部文件入口优先当前项目，其次当前 Profile 的 terminal.cwd；从对话进入时使用该对话的工作目录。不把 Profile 的配置文件目录当作工作目录，也不在失败后静默跳到服务器根目录。未设置有效路径时请通过文件页“切换项目”选择项目，或在服务器将 terminal.cwd 配置为绝对路径。

目录、文档请求显式携带 Profile，切换目录或 Profile 后忽略迟到响应。各 Profile 的项目选择按服务器分别保存。目录选择器仍允许用户明确浏览服务器目录来创建项目。

129 项测试覆盖既有会话与键盘问题，以及本次 Profile 目录、导航和页面回归。详细记录见 VALIDATION-3.1.3.md。原生预览使用示例数据；尚未连接用户真机或远程 Gateway。
