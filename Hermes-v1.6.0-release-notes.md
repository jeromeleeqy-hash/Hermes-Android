# Hermes Android 1.6.0

本次升级重新梳理移动端语音交互，并增加 Hermes Agent 语音模型管理。

## 语音入口

- 聊天页输入框内的麦克风只负责单次语音输入：点按开始录音，再次点按结束；识别文字写入输入框，是否自动发送由语音设置决定。
- 聊天页顶部的波形按钮是连续语音入口，两种操作不再共用导航逻辑。
- 连续语音页移除底部最近文字对话卡，只保留当前通话状态、录音、识别、播放、打断和错误恢复。

## Agent STT/TTS 设置

“我的 → 语音设置”可读取并保存当前 Hermes Profile 的语音配置：

- STT：启停、服务商、模型、中文优先或自动检测。
- TTS：服务商、模型和声音；支持自定义模型或声音标识。
- 测试：直接录音测试 Agent STT，或试听 Agent TTS；Android 系统识别测试独立保留。

配置通过 Hermes `/api/config` 写入当前 Profile。API Key 不在未加密的移动端连接中录入，请继续在服务器或 HTTPS WebUI 中管理。

## 默认建议

- 3.5 GB 左右内存的服务器先使用本地 Whisper `base + zh`；需要更高中文准确率时再升级到 `small + zh`。
- 免费 TTS 可使用 Edge TTS 的 `zh-CN-XiaoxiaoNeural`。
- 自动引擎优先使用 Agent；Agent STT 明确不可用时回退手机系统。选择“仅 Agent”后不会静默改用手机识别。

## 安装

版本号 `1.6.0`，`versionCode 160`，最低支持 Android 8.0。安装包沿用工程内固定开发签名，v0.3.1–v1.5.1 可直接覆盖安装。
