# Hermes Android 2.5.0

发布日期：2026-08-16

本次升级继续打磨 Telegram 式清爽交互，重点减少多余点击反馈、补齐会话手势，并让 Hermes Agent 的 Memory 与 Soul 更容易访问。

## 交互与手势

- 全局移除按钮、底栏、分段控件和列表项点击时短暂出现的灰色半透明矩形；选中底板、开关和页面切换原有的滑动、弹性动画继续保留。
- 首页所有下拉菜单与筛选选框之间增加 4 dp 间距，层级更清楚。
- 会话列表支持向左滑动，右侧露出“归档”和“删除”按钮；点击后仍保留确认步骤，降低误操作风险。

## Agent 与个人主页

- “我的”主页新增 Memory 和 Soul 两个核心入口：Memory 管理持久记忆、用户画像与上下文，Soul 管理人格、时区和表达方式。
- 点击头像后，同一张照片会从圆形小头像平滑扩展为顶部大图；返回时按原路径收回，不再通过整页缩放制造跳变。

## 兼容性

- 保留现有 Gateway、聊天、语音、会审、空间、任务和全部设置能力。
- 继续使用固定开发签名，可直接覆盖安装 v0.3.1 之后的版本。
