# Hermes 3.1.5 验证记录

- Gradle :app:testDebugUnitTest :app:assembleDebug 成功；150 项通过，0 失败 / 错误 / 跳过。
- 新增附件选择回归：项目文件及最近产物返回当前对话、保留草稿及原有附件、读取失败保留选择页、取消后迟到响应不污染另一会话。
- 文件路径回归覆盖：来源会话目录及档案、相对 Markdown 链接去重、中文路径解码、完整扩展名、旧索引恢复、权限错误、不存在文件及旧存储格式兼容。
- HTTP 回归验证空文件与显式档案；真实 WebSocket JSON-RPC 模拟验证 PDF 以服务器文件附件提交，不误用 image.attach_bytes。
- 原生交互回归验证：三点菜单精简、加号菜单打开命令选择器、空间两类文件均进入附件回调、回看按钮可见 45dp 且触摸区域至少 48dp。
- 原有并发会话、Profile 目录隔离、项目切换、历史 404、360dp / 1.3 倍字体键盘间距继续通过。
- Robolectric 原生图形截图覆盖首页、对话、回看、文件、附件选择、执行中心、设置、菜单及图标两类蒙版。导航选中图标采用空间上的蓝紫渐变；仍无选中底色或点击灰色背景。
- 首页输入区、对话输入区及文件等页面的导航固定区域上沿采用 1dp 同色系分隔线。首页不重复绘制两条固定区边界。
- 软件图标使用本次用户提供的托腮女孩参考图，经内置图像编辑工具处理白底；使用 24% 自适应前景 inset 保留人物边距，主题图标同步移除头盔样式。
- APK 签名、ZIP 完整性、16KB zipalign 通过，versionCode 315，versionName 3.1.5-release-debug；与此前交付测试版签名一致。

## 验证边界

未连接用户真机或远程 Gateway。服务器已删除或移动且来源消息无法提供有效路径的文件不能凭空恢复，此时显示具体提示。PDF、Word、表格等二进制附件引用当前档案服务器上的实际文件，处理能力取决于 Hermes 的文件工具与读取权限。原生截图使用测试内容，OEM 桌面蒙版与真实输入法仍需安装验证。

APK SHA-256：`b80ed5a5007bda547d2c418e094b0283d9fdbdecaad9a09a556065ef0c640a95`。
