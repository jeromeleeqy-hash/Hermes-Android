# Hermes Android v1.1.0

这一版把移动端从“远程聊天窗口”推进到更完整的 Agent 控制端：运行状态在全局可见，执行中可以补充要求，Hermes 需要确认或追问时也能直接在手机上处理。

## 新增

- 全局运行状态条：离开聊天页后继续显示当前执行阶段、待处理请求，并支持快速返回或停止。
- 执行中追加要求：通过 Hermes `session.steer` 将新指令送入当前运行。
- 排队发送：下一条消息会在本轮完成后自动开始，减少等待和重复进入对话。
- Agent 交互卡：支持审批、拒绝、会话级/永久允许，以及自由文本或选项式澄清。
- 完成结果卡：在回复末尾汇总本轮结果和产物入口。
- 通知深链：回复、审批和任务通知可以打开对应 Profile、对话或任务页面。
- “我的 → 更新日志”：在应用内直接查看版本新增和修复内容。

## 体验修复

- 从聊天打开 Markdown 产物再返回时，恢复离开前的阅读位置。
- 对话搜索同时匹配标题、摘要和来源，并明确搜索范围。
- 文件选择器不再显示当前读取器不支持的 PDF 类型。
- 用户与 Hermes 头像统一回到应用内置固定资源，移除照片选择入口。

## 兼容说明

- 需要支持 TUI Gateway `session.steer`、`approval.respond` 与 `clarify.respond` 的新版 Hermes Agent，旧版网关仍可继续使用基础聊天能力。
- Android 8.0 及以上，版本号 `1.1.0`，`versionCode 110`。
