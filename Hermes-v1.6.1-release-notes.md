# Hermes Android 1.6.1

本次小版本解决中文语音识别结果可能显示繁体的问题。

## 中文转写文字

进入“我的 → 语音设置 → 中文转写文字”，可选择：

- 简体中文（推荐）：升级后的默认值，统一把中文 STT 结果转换为简体。
- 繁体中文：统一转换为繁体。
- 保持识别原文：不修改 STT 服务商返回的文字。

该设置同时覆盖聊天输入框的单条语音、连续语音、Agent STT 测试和 Android 系统语音识别。手机系统识别还会随选项使用 `zh-CN` 或 `zh-TW`；Hermes Agent 侧继续保存兼容 Whisper 的 `zh` 语言参数，繁简转换由客户端完成，避免不同 STT 服务商行为不一致。

## 安装

版本号 `1.6.1`，`versionCode 161`，最低支持 Android 8.0。安装包沿用工程内固定开发签名，可覆盖 v0.3.1–v1.6.0。
