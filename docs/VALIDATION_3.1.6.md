# 3.1.6 验证记录

## 自动化与构建

- `testDebugUnitTest`：162 项通过，0 失败、0 错误、0 跳过。
- `assembleDebug`：生成独立预览 APK；应用 ID 为 `com.qingyu.hermescompanion.preview`，版本号 316，显示版本 `3.1.6-release-preview`。
- 构建环境：JDK 17、Gradle 8.13、Android Gradle Plugin 8.13.2、Android SDK 36。
- JVM 界面测试使用 Robolectric Android 15 环境；这些截图不等同于真机截图。

## 本次覆盖

- 有效讲话后静音自动结束，短暂停顿和噪声不会提前提交。
- 中文回答保留，Markdown 清理、长文本切段和中文声音能力筛选。
- 网关会话级思考开关与恢复、旧网关兼容，以及多会话隔离。
- 语音发送保留原文字草稿和附件；退出或换会话后丢弃迟到语音结果。
- 首页输入框和重复链接移除；小屏与大字体布局、末尾内容可滚动到达。
- 浅色/深色首页、回看、文件、附件选择和单条思考提示的 Compose 渲染。
- 圆形及连续圆角桌面图标掩模，以及悬浮导航的页面布局。

## 人工查看

已查看生成的浅色首页、深色首页、大字体首页、精简首页、文件页、回看页、附件选择页、思考状态和桌面图标预览。深色首页未出现白色矩形底；首页没有输入框和重复的“接着聊聊”链接。

## 尚未真机验证

本次环境没有接入用户的手机、Hermes 网关或 DeepSeek 服务。麦克风噪声与停顿阈值、手机中文发音人、扬声器回声、系统语音识别界面，以及真实服务端的首字延迟需要真机验证。快速回答以服务端支持会话级思考设置为前提；不会承诺具体响应时长。

预览包采用独立签名与应用 ID，可以和旧 App 并存。首次使用需要重新连接网关；不能直接覆盖旧测试包。恢复旧测试包的签名文件后，可用源代码按原应用 ID 构建覆盖升级包。

## 安装包校验

- APK v2 签名验证通过。
- APK SHA-256：`73681e905a5b140a2057b7cc9e7e3e2fb9bbeab52a4291a8d13e40e96af4c700`
- 签名证书 SHA-256：`741c1844bb9267547e06af30d56c0da5bfcbf01491d3670496d88837a2860ad6`。
