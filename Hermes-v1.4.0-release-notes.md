# Hermes Android 1.4.0

第四批功能聚焦移动端 Agent 控制、语音交互与服务器维护。

## 新功能

- Agent 执行中心：任务页增加待处理、进行中、定时任务和执行记录四个视图。
- 交互收件箱：审批与澄清请求可以跨页面集中处理，通知可快速打开处理页或来源会话。
- 执行记录：保留最近对话任务的结果摘要和产物入口；运行卡展示阶段、耗时、断线恢复、打开和停止操作。
- 语音对话：优先调用服务器 Hermes STT/TTS，缺失时回退到 Android 语音能力；支持自动朗读、打断和连续对话。
- Agent 维护：查看当前 Agent 版本、安装方式、落后提交与更新说明，并从 APP 发起官方 Gateway 更新。

## 安全与兼容

- APP 不通过 SSH 执行任意服务器命令，只调用 Hermes 官方 `/api/hermes/update` 接口。
- 更新期间允许 Gateway 正常重启，客户端最长等待约 5 分钟并反复重连、强制检查版本，避免把暂时断联误报成失败。
- 不支持远程更新的 Docker 或外部托管安装只显示官方建议命令，不会绕过服务器限制。
- 语音数据只发送到当前连接的 Hermes Gateway；服务器语音接口不可用时可选择手机系统服务。

## 安装

版本号 `1.4.0`，`versionCode 140`，最低支持 Android 8.0。安装包沿用工程内固定开发签名，v0.3.1–v1.3.0 可直接覆盖安装。
